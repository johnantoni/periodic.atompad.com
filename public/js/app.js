$(document).ready(function () {

	// get json elements
	// get json chemicals
	
	// render elements as ([a] [figure])
	// ..links jump to wikipedia
	// ..use elements extra data to add class (color, extra details)
	
	// render chemicals as table
	// ..use extra data as before

	// render cleanly

});
